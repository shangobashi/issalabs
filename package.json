{
  "name": "issalabs-preview",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx serve .",
    "deploy:preview": "vercel deploy -y",
    "deploy:prod": "vercel deploy --prod -y"
  }
}
